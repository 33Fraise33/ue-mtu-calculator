services:
  app:
    image: node:22.12.0-bookworm
    working_dir: /app
    ports:
      - "5173:5173"
      - "4173:4173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev -- --host 0.0.0.0

volumes:
  node_modules:
